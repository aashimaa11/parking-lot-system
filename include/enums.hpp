#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class VehicleType {
    CAR,
    BIKE,
    TRUCK,
    ELECTRIC
};

enum class SpotType {
    COMPACT,
    LARGE,
    HANDICAPPED,
    ELECTRIC
};

enum class TicketStatus {
    ACTIVE,
    PAID,
    LOST
};

#endif